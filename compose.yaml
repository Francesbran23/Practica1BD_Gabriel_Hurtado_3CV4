services:
  db:
    image: postgres:16
    container_name: pg-practica1
    environment:
      POSTGRES_PASSWORD: practica1
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: